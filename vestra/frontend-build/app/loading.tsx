/** Root loading fallback — shown instantly while page navigation happens. */
import { LoadingScreen } from '@/components/ui/card';

export default function RootLoading() {
  return <LoadingScreen />;
}
